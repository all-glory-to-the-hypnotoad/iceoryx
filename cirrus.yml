test_task:
  matrix:
    - container:
        image: gcc:latest
    - allow_failures: true
      container:
        image: clang:latest
  build_script: tools/iceoryx_build_test.sh build-strict build-all out-of-tree build-shared
  test_script: cd build && ./tools/run_tests.sh all
